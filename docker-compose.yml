version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: bookreview_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped
    command: ["--wiredTigerCacheSizeGB", "1"]  

volumes:
  mongo_data: